/**
 * this is type is used to make sure the required properties are present
 */
type apinterface = {
  endpoint: string;  // this should be a domain name or an IP address
  token: string;  // this should be the api token for that device
};
